const startMoney = 100000;
const productionDivider = 1000 * 60 * 60; // ms per hour
const consumptionDivider = 25000 // to define

module.exports = {
  startMoney,
  productionDivider,
  consumptionDivider
};
