import React from 'react';

import PageHeader from '../common/PageHeader';
import PageContainer from '../common/PageContainer';

function Galaxy() {
  return (
    <PageContainer>
      <PageHeader height={'450px'} imgWidth={'400px'} imageName={'galaxy'} title={'Galaxy'} />
    </PageContainer>
  );
}

export default Galaxy;
